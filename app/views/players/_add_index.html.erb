<%= will_paginate %>

<%= form_tag trash_players_path do %>

<table>
    <thead>
    	<tr>
            <th></th>
    		<th>Name</th>
    		<th>Email</th>
    		<th>Skill</th>
    		<th>Matches Won</th>
    	</tr>
    </thead>
    <% if Player.any? %>
    <tbody>
    	<% @players.order("created_at DESC").each do |p| %>
        <tr>
        	<% temp = "#{p.first_name} #{p.last_name}" %>
            <td><%= check_box_tag "player_ids[]", p.id %></td>
            <td><%= link_to "#{temp}", player_path(p) %></td>
            <td><%= p.email %></td>
            <td><%= p.skill %></td>
            <td><%= p.matches_won %></td>
        </tr>
	    <% end %>
    </tbody>
    <% end %>
</table>
<%= hidden_field_tag :tournament_id, value: @tournament.id %>
<%= submit_tag "Delete checked" %>
<% end %>

<%= will_paginate %>