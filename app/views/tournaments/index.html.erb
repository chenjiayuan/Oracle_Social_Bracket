<body>
	<h3>Tournaments</h3>
<div id="tournaments">
<% if Tournament.any? %>
    <%= pluralize(Tournament.count, 'tournament') %> have been created so far<br />
    <% Tournament.all.each do |t| %>
        Name: <%= t.name %><br />
        Player Count: <%= t.player_count %><br />
        <%= link_to "Show", t %><br />
    <% end %>
<% end %>
</div>

<div class='create-tour'><%= link_to "Create Tournament", new_tournament_url %></div>

</body>

