<body>
	<h3>Tournaments</h3>
	<div id="bound"></div>
<% if Tournament.any? %>
    <%= pluralize(Tournament.count, 'tournament') %> have been created so far<br />
    <% Tournament.all.each do |t| %>
        Name: <%= t.name %><br />
        Player Count: <%= t.player_count %>
    <% end %>
<% end %>

<div class='create-tour'><%= button_tag "Create tournament" %></div>
</body>